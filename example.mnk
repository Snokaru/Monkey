let add = fn(x, y) {
    x + y;
};

let double = fn(x) {
    x * 2;
};

let operation = add(2, 3) - double(2);

if (operation > 2) {
    return 'the operation is greater than 2';
} else {
    return 'the operation is not greater than 2';
}